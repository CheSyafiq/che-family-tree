rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow everyone to read members, but only authenticated users can write
    match /members/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
